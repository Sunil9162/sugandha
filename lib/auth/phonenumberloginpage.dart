import 'package:flutter/material.dart';

class PhoneNumberLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PhoneNumberLogin'),
      ),
      body: Center(
        child: Container(
          child: Text('PhoneNumberLogin'),
        ),
      ),
    );
  }
}
